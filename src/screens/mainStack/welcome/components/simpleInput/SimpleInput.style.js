import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    borderWidth: 1,
    alignSelf: 'center',
    paddingHorizontal: 15,
    textAlign: 'center',
    fontSize: 20,
    marginTop: 10,
  },
});
