import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scrollView: {
    flexDirection: "row",
    backgroundColor: "#00d4ff",
    
  },
  loadingIndicator:{
    flex: 1,
  }
});
